# Copy this file to .env.local and fill in real values.
# .env.local is gitignored and should never be committed.

# API key for the AI provider used by the chat feature (added in a later phase)
OPENAI_API_KEY=

# Public site URL, used for absolute fetches in Server Components
NEXT_PUBLIC_SITE_URL=http://localhost:3000
